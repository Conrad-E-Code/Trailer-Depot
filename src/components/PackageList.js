

function PackageList(props) {
    return(
        <div>
            PACKAGE LIST HERE
            {/* perform a fetch from /packages endpoint and render the packages with package cards you may have to make a new card */}
        </div>
    )
}

export default PackageList